spring:
  application:
    name: "myFisrtProject"
  datasource:
    url: jdbc:mysql://localhost:3306/store?createDatabaseIfNotExist=true
    username: root
    password: Leasts@4321
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    database-platform: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        show_sql: true
        format_sql: true
stripe:
  apiUrl: https://stripe.come
  timeout: 1000
  enabled: true
  supported-currencies: USD,EUR,GBP
paymentGateway: "payPal"
userRegisteration:
  port: 1234
  host: https://alibaba.com
